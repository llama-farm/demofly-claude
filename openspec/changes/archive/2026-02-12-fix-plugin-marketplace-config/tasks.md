## 1. Fix marketplace.json

- [x] 1.1 Add `$schema`, `version`, and `owner` block to `.claude-plugin/marketplace.json`
- [x] 1.2 Add `version`, `category`, and `tags` to the demofly plugin entry in `marketplace.json`

## 2. Fix plugin.json

- [x] 2.1 Add `author`, `repository`, `license`, and `keywords` to `plugins/demofly/.claude-plugin/plugin.json`
